---
const { title = await Astro.slots.render('default'), subtitle = await Astro.slots.render('subtitle') } = Astro.props;
---

<header class="mb-3 md:mb-6 max-w-5xl mx-auto">
  <h1 class="text-white font-heading text-3xl md:text-4xl lg:text-5xl leading-tighter tracking-tighter" set:html={title} />
  {
    subtitle && (
      <div class="mt-2 md:mt-3 mx-auto text-xl text-gray-500 dark:text-neutral-400 font-medium" set:html={subtitle} />
    )
  }
</header>
